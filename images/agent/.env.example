# Agent Container Environment Variables
# Copy this to .env and fill in the values

# Required: Tailscale authentication key
# Get from: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-xxx

# Required: GitHub credentials for cloning repos
GH_USERNAME=your-github-username
GH_TOKEN=ghp_xxx

# Optional: Timezone
TZ=UTC

# Optional: Git configuration
GIT_EMAIL=agent@coordinator.local
GIT_NAME=Agent

# Optional: Dotfiles repository for shell configuration
DOTFILES_REPO=your-username/dotfiles

# Optional: Manager WebSocket URL (auto-set by create-agent)
# MANAGER_WS_URL=ws://manager.tailnet.ts.net:3100

# Set by create-agent script (do not set manually):
# DEV_HOSTNAME - Container hostname
# WORKSPACE_REPO - GitHub repo to clone
# WORKSPACE_BRANCH - Branch to checkout
# TS_WG_PORT - WireGuard port for direct connections
# TS_TUN_NAME - Custom TUN interface name
